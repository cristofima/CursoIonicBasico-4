import { SeparatorMilesPipe } from './separator-miles.pipe';

describe('SeparatorMilesPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparatorMilesPipe();
    expect(pipe).toBeTruthy();
  });
});
